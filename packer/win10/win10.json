{
  "variables": {
    "boot_wait": "5s",
    "accelerator": "kvm",
    "autounattend": "./scripts/bios/autounattend.xml",
    "cpus": "4",
    "disk_size": "61440",
    "headless": "true",
    "iso_checksum": "743fc483bb8bf1901c0534a0ae15208a5a72a3c5",
    "iso_checksum_type": "sha1",
    "iso_url": "https://software-download.microsoft.com/download/pr/18362.30.190401-1528.19h1_release_svc_refresh_CLIENTENTERPRISEEVAL_OEMRET_x64FRE_en-us.iso",
    "memory_size": "4096",
    "shutdown_command": "%WINDIR%/system32/sysprep/sysprep.exe /generalize /oobe /shutdown /unattend:C:/Windows/Temp/Autounattend.xml",
    "winrm_password": "packer",
    "winrm_username": "Administrator",
    "vm_name": "DESKTOP-WIN10",
    "vm_id": 48451,
    "virtio_iso_path": "../virtio-win-0.1.229.iso",
    "proxmox_disk_size": "40G"
  },
  "builders": [
    {
      "type": "qemu",
      "accelerator": "{{ user `accelerator` }}",
      "boot_wait": "20s",
      "communicator": "winrm",
      "cpus": "{{ user `cpus`}}",
      "disk_compression": "true",
      "disk_interface": "virtio",
      "vm_name": "{{user `vm_name`}}.qcow2",
      "disk_size": "{{user `disk_size`}}",
      "cd_files": [
        "{{user `autounattend`}}",
        "./scripts/fixnetwork.ps1",
        "./scripts/install-choco.ps1",
        "./scripts/install-misc.bat",
        "./scripts/redhat-cert.cer",
        "./scripts/redhat-cert-old.cer",
        "./scripts/enable-winrm.ps1",
        "./scripts/bios/Firstboot-Autounattend.xml",
        "./scripts/user-profile.ps1",
        "./drivers/"
      ],
      "cd_label": "scripts",
      "format": "qcow2",
      "headless": "{{user `headless`}}",
      "iso_checksum": "{{user `iso_checksum_type`}}:{{user `iso_checksum`}}",
      "iso_url": "{{user `iso_url`}}",
      "memory": "{{ user `memory_size`}}",
      "net_device": "virtio-net",
      "qemuargs": [
        [
          "-cdrom",
          "{{user `virtio_iso_path`}}"
        ],
        [
          "-vga",
          "qxl"
        ]
      ],
      "shutdown_command": "{{ user `shutdown_command` }}",
      "winrm_insecure": "true",
      "winrm_timeout": "120m",
      "winrm_username": "{{user `winrm_username`}}",
      "winrm_password": "{{user `winrm_password`}}",
      "winrm_use_ssl": "true"
    },
    {
      "type": "proxmox-iso",
      "proxmox_url": "{{user `proxmox_url`}}",
      "username": "{{user `proxmox_token_id`}}",
      "token": "{{user `proxmox_token_secret`}}",
      "node": "{{user `proxmox_node`}}",
      "vm_name": "{{user `vm_name`}}",
      "memory": "{{ user `memory_size`}}",
      "cores": "{{user `numvcpus`}}",
      "os": "win10",
      "tags": "advenv",
      "insecure_skip_tls_verify": "{{user `insecure_skip_tls_verify`}}",
      "machine": "q35",
      "scsi_controller": "virtio-scsi-single",
      "network_adapters": [
        {
          "model": "e1000",
          "bridge": "vmbr0"
        }
      ],
      "disks": [
        {
          "type": "scsi",
          "disk_size": "{{user `proxmox_disk_size`}}",
          "storage_pool": "{{user `proxmox_storage`}}"
        }
      ],
      "boot_iso": {
        "type": "scsi",
        "iso_file": "{{user `proxmox_iso_storage`}}:iso/18362.30.190401-1528.19h1_release_svc_refresh_CLIENTENTERPRISEEVAL_OEMRET_x64FRE_en-us.iso",
        "unmount": true,
        "iso_checksum": "{{user `iso_checksum`}}"
      },
      "additional_iso_files": [
        {
          "type": "sata",
          "iso_file": "{{user `proxmox_iso_storage`}}:iso/win10-unattend.iso",
          "unmount": true
        },
        {
          "type": "sata",
          "iso_file": "{{user `proxmox_iso_storage`}}:iso/virtio-win-0.1.229.iso",
          "unmount": true
        },
        {
          "type": "sata",
          "unmount": false,
          "cd_label": "scripts",
          "cd_files": [
            "./scripts/fixnetwork.ps1",
            "./scripts/install-choco.ps1",
            "./scripts/install-misc.bat",
            "./scripts/redhat-cert.cer",
            "./scripts/redhat-cert-old.cer",
            "./scripts/enable-winrm.ps1",
            "./scripts/bios/Firstboot-Autounattend.xml",
            "./scripts/user-profile.ps1",
            "./drivers/"
          ],
          "iso_storage_pool": "{{user `proxmox_iso_storage`}}"
        }
      ],
      "boot_wait": "{{user `boot_wait`}}",
      "http_directory": "scripts/bios",
      "boot_command": [
        "<esc><wait><f12><wait><enter>"
      ],
      "communicator": "winrm",
      "winrm_username": "{{user `winrm_username`}}",
      "winrm_password": "{{user `winrm_password`}}",
      "winrm_use_ssl": true,
      "winrm_insecure": true,
      "winrm_timeout": "4h",
      "qemu_agent": true,
      "template_name": "{{user `vm_name`}}",
      "template_description": "Windows 10 template built on {{timestamp}}"
    }
  ],
  "provisioners": [
    {
      "execute_command": "{{.Vars}} cmd /c C:/Windows/Temp/script.bat",
      "remote_path": "c:/Windows/Temp/script.bat",
      "scripts": [
        "./scripts/disable-auto-logon.bat",
        "./scripts/enable-rdp.bat",
        "./scripts/compile-dotnet-assemblies.bat",
        "./scripts/install-misc.bat",
        "./scripts/compact.bat"
      ],
      "type": "windows-shell"
    }
  ]
}
